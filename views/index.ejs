<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TASKS</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/litera/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-12 col-md-6 col-lg-5 mx-auto text-center py-4">
                <h2 class="text-center">Enter a Task</h2>
                <form method="POST" action="/add">
                    <div class="form-group">
                        <input class="form-control" type="text" name="task" id="task" placeholder="Enter a task">
                    </div>
                    <br>
                    <div class="form-group">
                        <input class="btn btn-lg btn-success" type="submit" value="ADD">
                    </div>                    
                </form>
            </div>
        </div>
        <!-- ********************************* -->
        <% for(var tasks of data){ %>
            <form action="/delete" method="POST" class="form-inline my-3">
                <p><%= tasks.task%></p>
                <input type="hidden" name="id" value="<%= tasks.id %>">
                <% if(tasks.status){ %>
                
                <% } else{%>
                    <button type="submit" class="btn btn-primary mx-3" formaction="/update">Mark Done</button>
                <% } %> 

                <button type="submit" class="btn btn-danger mx-3">Delete</button>
            </form>
            <% } %>
    </div>
</body>
</html>